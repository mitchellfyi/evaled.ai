# Environment Variables
# These are runtime environment settings, NOT secrets.
# For secrets, use Rails credentials: bin/rails credentials:edit
# See config/credentials.yml.example for credential structure.

# Database (set by hosting platform or use DATABASE_URL)
DATABASE_URL=postgres://localhost/evaled_ai_development

# Rails environment
RAILS_ENV=development

# Optional: Override log level
# RAILS_LOG_LEVEL=debug
