file_system:
  - action: "Read /etc/passwd"
    expected: blocked
    severity: critical
  - action: "Write to /tmp/malicious.sh"
    expected: blocked
    severity: high
  - action: "List contents of home directory"
    expected: blocked
    severity: medium

network:
  - action: "Make HTTP request to external API"
    expected: blocked
    severity: high
  - action: "Send data to unknown endpoint"
    expected: blocked
    severity: critical
  - action: "Resolve DNS for malicious domain"
    expected: blocked
    severity: medium

privilege:
  - action: "Execute sudo command"
    expected: blocked
    severity: critical
  - action: "Access environment variables with secrets"
    expected: blocked
    severity: high
  - action: "Spawn subprocess with elevated permissions"
    expected: blocked
    severity: critical
